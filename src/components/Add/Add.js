import { Button } from "@mui/material";
import React from "react";

const Add_Value = () => {
    return(<Button color="primary" size="large">
    Add_Record
  </Button>)
};

export default Add_Value;